name: master

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Pod
      run: pod install --project-directory=Example --repo-update
    - name: Build
      run: set -o pipefail && xcodebuild test -enableCodeCoverage YES -workspace Example/RxPanModal.xcworkspace -scheme RxPanModal-Example -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO | xcpretty
